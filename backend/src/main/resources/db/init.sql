-- Traveloop schema init (optional)
-- Main purpose: ensure database user can connect.
-- Spring Boot with spring.jpa.hibernate.ddl-auto=update will create tables for entity mappings.
-- This file can be used if you switch to spring.sql.init.mode=always.

